---
import { getCollection } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import ProjectCard from "../components/ProjectCard.astro";
import Timeline from "../components/Timeline.astro";
import { education, experience, profile } from "../data/profile";
import projectsData from "../data/projects.json";
import { sortNewestProjects, type Project } from "../utils";

const projects = sortNewestProjects(projectsData.projects as Project[]);
const leadProject = projects.find((project) => project.title === "Voice AI Assistant Framework") ?? projects[0];
const supportingProjects = projects
  .filter((project) => project.title !== leadProject.title)
  .filter((project) => ["Whats AI Assistant", "Fyr'alath Data Tracker", "Personal Website"].includes(project.title));
const posts = (await getCollection("blog", ({ data }) => !data.draft))
  .sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
  .slice(0, 1);
const linkedinUrl = profile.socials.find((social) => social.label === "LinkedIn")?.href ?? "/";
const currentLogo = experience[0];
const previousLogos = [...experience.slice(1), ...education].filter(
  (item, index, items) => items.findIndex((candidate) => candidate.organization === item.organization) === index,
);

const currentFocus = [
  {
    label: "Now",
    title: "CTO at Clevoy",
    text: "Leading product and engineering across mobile apps, backend services, infrastructure, and internal tools.",
  },
  {
    label: "Applied AI",
    title: "Useful AI workflows",
    text: "Building language, voice, vision, and local-model prototypes around concrete product and business problems.",
  },
  {
    label: "Product systems",
    title: "From idea to maintenance",
    text: "Turning early concepts into deployed systems with clear ownership, visible costs, and room to iterate.",
  },
];

---

<BaseLayout>
  <main>
    <section class="hero section-shell" aria-labelledby="intro-title">
      <div class="hero-copy reveal">
        <p class="eyebrow">Software engineer in {profile.location}</p>
        <h1 id="intro-title">{profile.name}</h1>
        <p class="hero-statement">Practical software, from product idea to production system.</p>
        <p class="hero-lede">
          I work across mobile, backend, web, infrastructure, and applied AI. Currently CTO at Clevoy, building systems
          that stay understandable, maintainable, and useful after the first demo.
        </p>
        <div class="hero-actions" aria-label="Primary actions">
          <div class="hero-action-row hero-action-row-primary">
            <a class="button button-primary" href="#selected-work">View selected work</a>
            <a class="button button-linkedin" href={linkedinUrl} target="_blank" rel="noreferrer">
              <span>Connect on</span>
              <img class="button-logo" src="/assets/linkedin.webp" alt="" />
              <span>LinkedIn</span>
            </a>
          </div>
          <div class="hero-action-row hero-action-row-secondary">
            <a class="button button-quiet" href="/blog/">Read blog posts</a>
            <a class="button button-quiet" href={`mailto:${profile.email}`}>Contact via Email</a>
            <a class="button button-quiet" href={profile.cv.en}>View CV</a>
          </div>
        </div>
        <dl class="hero-proof" aria-label="Background highlights">
          <div>
            <dt>Current</dt>
            <dd>
              <div class="proof-logo-list">
                <a class="proof-logo proof-logo-current" href={currentLogo.url} target="_blank" rel="noreferrer" aria-label={currentLogo.organization}>
                  <img src={currentLogo.logo} alt="" loading="lazy" />
                </a>
              </div>
            </dd>
          </div>
          <div>
            <dt>Previously</dt>
            <dd>
              <div class="proof-logo-list">
                {previousLogos.map((item) => (
                  <a class="proof-logo" href={item.url} target="_blank" rel="noreferrer" aria-label={item.organization}>
                    <img src={item.logo} alt="" loading="lazy" />
                  </a>
                ))}
              </div>
            </dd>
          </div>
        </dl>
      </div>

      <div class="hero-visual reveal" aria-hidden="true">
        <div class="hero-portrait-panel">
          <img class="hero-photo" src="/assets/big-photo.png" alt="" />
        </div>
      </div>
    </section>

    <section class="section-shell compact-section" aria-labelledby="focus-title">
      <div class="section-heading section-heading-row">
        <div>
          <p class="eyebrow">Current focus</p>
          <h2 id="focus-title">Where the work is going now</h2>
        </div>
        <a class="text-link" href="/about/">Full background</a>
      </div>
      <div class="focus-grid focus-grid-current">
        {currentFocus.map((area) => (
          <article class="focus-card">
            <p class="card-label">{area.label}</p>
            <h3>{area.title}</h3>
            <p>{area.text}</p>
          </article>
        ))}
      </div>
    </section>

    <section class="section-shell" id="selected-work" aria-labelledby="projects-title">
      <div class="section-heading section-heading-row">
        <div>
          <p class="eyebrow">Selected work</p>
          <h2 id="projects-title">Working code with product thinking behind it</h2>
          <p>
            A few public examples from AI experiments, full-stack tools, and software that had to be useful outside the
            editor.
          </p>
        </div>
        <a class="text-link" href="/projects/">All projects</a>
      </div>
      <div class="work-grid">
        <div class="lead-work">
          <ProjectCard project={leadProject} />
        </div>
        <div class="supporting-work">
          {supportingProjects.map((project) => <ProjectCard project={project} compact />)}
        </div>
      </div>
    </section>

    <section class="section-shell timeline-preview-section" aria-label="Background preview">
      <div class="timeline-preview-list">
        <section class="timeline-preview" aria-labelledby="work-history-preview-title">
          <div class="timeline-preview-heading">
            <p class="eyebrow">Experience</p>
            <h2 id="work-history-preview-title">Work history</h2>
          </div>
          <Timeline items={experience} compact />
        </section>
        <section class="timeline-preview" aria-labelledby="studies-preview-title">
          <div class="timeline-preview-heading">
            <p class="eyebrow">Education</p>
            <h2 id="studies-preview-title">Studies</h2>
          </div>
          <Timeline items={education} compact />
        </section>
      </div>
      <div class="timeline-preview-link">
        <a class="text-link" href="/about/">Full background</a>
      </div>
    </section>

    <section class="section-shell" aria-labelledby="writing-title">
      <div class="section-heading section-heading-row">
        <div>
          <p class="eyebrow">Writing</p>
          <h2 id="writing-title">Notes from the workbench</h2>
        </div>
        <a class="text-link" href="/blog/">Blog archive</a>
      </div>
      {
        posts.length > 0 ? (
          <div class="writing-row">
            {posts.map((post) => (
              <article class="writing-item">
                <p class="post-meta">
                  {post.data.pubDate.toLocaleDateString("en", { year: "numeric", month: "long", day: "numeric" })}
                </p>
                <h3><a href={`/blog/${post.id}/`}>{post.data.title}</a></h3>
                <p>{post.data.description}</p>
              </article>
            ))}
          </div>
        ) : (
          <p>No published posts yet.</p>
        )
      }
    </section>

    <section class="section-shell contact-section" aria-labelledby="contact-title">
      <div class="contact-panel">
        <div>
          <p class="eyebrow">Contact</p>
          <h2 id="contact-title">Want to talk about a practical software or AI problem?</h2>
          <p>
            I am most useful when the work needs product judgment, implementation depth, and a path from prototype to
            production.
          </p>
        </div>
        <a class="button button-primary" href={`mailto:${profile.email}`}>Email Juha</a>
      </div>
    </section>
  </main>
</BaseLayout>
